# Архитектура автоматизации

Как система из 10 промптов превращается в воспроизводимый конвейер и что для
этого нужно дописать. Документ описывает **целевое** устройство; фазы 1, 2 и
часть 6 уже реализованы — см. §8.

## 1. Что происходит сейчас

Разбор — это десять сообщений в чате, отправляемых по порядку. Между ними
человек руками переносит:

* технический экспорт с vedic-horo (этап 01) — скриншотами или текстом;
* сводку каждого этапа в начало следующего;
* биографию (этап 03);
* итоговый markdown в HTML-шаблон и в PDF (этап 10).

Отсюда все известные сбои версии 1.0, перечисленные в `00_README`: этап 01
недоделывался, гейт биографии проскакивался, упаи не дописывались. Это не
дефекты формулировок — это дефекты **ручной передачи состояния между шагами**.
Автоматизировать нужно именно передачу, а не мышление.

## 2. Ключевое изменение: этап 01 больше не ручной

`00_README` утверждает:

> ИИ не может зайти на сайт сам (нужна интерактивная форма расчёта)… планируйте
> этап 01 как ручной.

Это перестало быть правдой. [`serg123e/vedic-parser`](https://github.com/serg123e/vedic-parser)
ходит не в интерактивную форму, а напрямую в `actions.php`, который отдаёт
HTML-фрагменты без Cloudflare-челленджа и без авторизации. Карта задаётся
параметрами (`name/date/time/timezone/latitude/longitude`), ответы разбираются
в JSON.

**Следствие:** этап 01 переходит из категории «ручной» в категорию «полностью
автоматический», а его результат — из «то, что успел прислать пользователь» в
«детерминированный экспорт с фиксированным составом». Это самый крупный выигрыш
во всей автоматизации, и он на 70% уже написан.

Текст `00_README` в этой части устарел и требует правки (см. §8).

## 3. Четыре класса работы

Десять промптов однородны только на вид. Внутри — четыре разных типа работы, и
автоматизируются они по-разному.

| Класс | Где | Кто исполняет |
|---|---|---|
| **A. Сбор данных** | 01 | Код. Модель не участвует вообще |
| **B. Арифметика** | 07 (веса × баллы), производные аштакаварги, арудхи, диспозиторы, гандантa | Код. Модель даёт входы, но не считает |
| **C. Интерпретация** | 02, 04, 05, 06, 08, 09 | Модель |
| **D. Проверки и гейты** | 03 (гейт), 07 (допуск), 10 (27 пунктов), правила `00_README` | Код там, где правило формальное; модель-судья там, где смысловое |
| **E. Человек** | 03 (биография), статус времени рождения, приоритетные сферы | Только человек |

Разделение B и C — принципиальное. Промпт 07 прямо запрещает назначать итоговое
число «по общему впечатлению»: значит модель выставляет шесть баллов слоёв с
обоснованием, а взвешенную сумму, подшкалы и таблицу вкладов считает код. Тогда
арифметика не может разойтись с таблицей, а таблица — с числом.

То же в D. «Гейт биографии» и «условие допуска к 07» сегодня держатся на
дисциплине читателя. В конвейере это условие в коде: этап 07 отказывается
стартовать, если статус времени рождения не `documented` и не `rectified` с
указанием, кем и по каким событиям, а
этап 04 помечает все паттерны как непроверенные, если нет `biography.md`.

## 4. Состояние вместо копипаста

Сводки перестают быть текстом, который переносят руками, и становятся файлами.
Каждый этап **читает** нужные ему сводки и **пишет** свою. Порядок и состав
контекста задаются не человеком, а таблицей зависимостей:

```
01 → (raw)                    02 ← 01
03 ← 01,02  + biography.md    04 ← 01,02,03
05 ← 01,03,04                 06 ← 02,03,04,05
07 ← 01,03,06  [гейт]         08 ← 01,03,04,07
09 ← 02…08                    10 ← 09
```

Отдельно от прозы живёт **`patterns.json`** — те самые 10 паттернов. У них
фиксированная схема (название, опора, уверенность, подтверждения с указанием
слоя, противоречащие факторы, статус проверки биографией). Этап 02 их создаёт,
03 меняет статусы и опору, 09 рендерит. Когда это структура, а не абзацы:

* правило «≥3 подтверждения из **разных** слоёв» проверяется кодом, а не на
  доверии — слой у каждого подтверждения указан явно;
* правило пересмотра из этапа 03 видно в диффе: что было, что стало, почему;
* маркер «подтверждено биографией» в отчёте берётся из поля, а не из памяти.

## 5. Устройство репозиториев

Границу проводим по природе данных: `vedic-parser` знает про сайт и ничего не
знает про методику; `jyotish-map` знает методику и ничего не знает про HTML.

```
vedic-parser/                  внешняя зависимость, чистые данные
└── vedic_parser/              session, show-info/chart/other/dasha/bala …

jyotish-map/                   маркетплейс плагинов Claude Code
├── .claude-plugin/marketplace.json
├── .agents/skills/            путь обнаружения по открытому стандарту
└── plugins/jyotish/           один плагин, самодостаточный
    ├── .claude-plugin/plugin.json
    ├── skills/jyotish-reading/   бандл Agent Skills, читают Claude и ChatGPT
    │   ├── SKILL.md           порядок этапов, гейты, два режима работы
    │   ├── references/        01…10 — методика, единственный источник правды
    │   ├── assets/            вёрстка PDF
    │   └── scripts/jyotish    обёртка CLI с деградацией в ручной режим
    ├── commands/              /jyotish:collect, /jyotish:stage … (только Claude)
    ├── jyotish/
    │   ├── collect.py         этап 01: парсер → raw/*.json (с кэшем)
    │   ├── derive.py          класс B: арудхи, диспозиторы, чара-караки
    │   ├── render_raw.py      raw/*.json → RAW_DATA.md + MISSING_DATA.md
    │   ├── soul_path.py       этап 07: веса × баллы, подшкалы, таблица
    │   ├── patterns.py        10 паттернов и правило независимости
    │   ├── validate.py        класс D: чек-лист 10 + правила README
    │   ├── crosscheck.py      второй независимый расчёт и разбор расхождений
    │   ├── sensitivity.py     карта на ±N минут: интервал устойчивости варг и даш
    │   ├── yogas.py           йоги с арифметическим условием: Кала-сарпа по градусам, сожжение, Нича-бханга
    │   ├── progress.py        состояние этапов по файлам, зависимости, следующий шаг
    │   └── report.py          md → html по шаблону → pdf, оглавление по фактическим страницам
    └── tests/
clients/<slug>/                состояние разбора, в .gitignore
```

Плагин самодостаточен намеренно: `${CLAUDE_PLUGIN_ROOT}` не дотягивается за
пределы каталога плагина, поэтому промпты, шаблон и код лежат внутри него, а не
в корне репозитория.

Методика живёт **внутри бандла скилла**, а не рядом с ним, по той же причине в
квадрате: бандл Agent Skills загружается в ChatGPT одним архивом и должен быть
полным сам по себе. Копия одна, `.agents/skills/` — ссылка на неё. Вендорные
подстановки в `SKILL.md` запрещены и проверяются тестом: с ними бандл молча
перестаёт работать везде, кроме одного агента.

Состояние одного разбора:

```
clients/<slug>/
├── chart.yaml            дата/время/место + статус времени рождения
├── raw/*.json            кэш ответов сайта, по файлу на действие+варгу
├── 01_RAW_DATA.md  01_MISSING_DATA.md
├── biography.md          заполняет человек; нет файла → гейт закрыт
├── state/patterns.json   10 паттернов, живут через 02 → 03 → 09
├── state/summaries/02.md … 08.md
├── stages/02.md … 09.md  полные тексты этапов
├── soul_path.json        входы и результат расчёта этапа 07
└── report.md  report.html  report.pdf  qa.md
```

`clients/` не коммитится: там персональные данные живых людей.

## 6. Что парсер уже закрывает, а что нет

Сверка 19 разделов Промпта 01 с тем, что реально отдаёт `vedic-parser` сегодня.

| § Промпта 01 | Источник | Статус |
|---|---|---|
| 1. Параметры расчёта | вход + `show-other.points.ayanamsa` | ✅ частично: айанамша есть; стиль карты, узлы, система карак анонимно = дефолты сайта, их надо зафиксировать явно |
| 2. D1 полностью | `show-info` + `show-chart` | ✅ знак, градус, дом, накшатра/пада/управитель, навамша, достоинство, ретро, аспекты, карака, функц. статус, бинду, шадбала. Диспозитор выводится локально |
| 3. Все варги | `show-info` + `show-chart` × D1…D60 | ✅ циклом. Ловушка: знаки варги только из `show-chart`, в `show-info` они остаются натальными |
| 4. Накшатры | `show-info` + `show-other.points` | ✅ 22-я дреккана, 64-я навамша, виша-навамша, сарпа-дреккана; гандантa и пушкара-навамша приходят кодами в `position`. ❌ абхукта-мула |
| 5. Джаймини | `show-info.karaka`, `lagnas.karakamsa_lagna`, `get-aspects` | ✅ караки, каракамша, раши-дришти по 12 знакам |
| 6. Арудхи | `jyotish/derive.py` | ✅ **[расчёт]** A1–A12, AL, UL, Дара-пада: сайт не отдаёт, считаются локально |
| 7. Спец. лагны и точки | `show-other.lagnas` (15) + `upagrahas` (11) | ✅ список Промпта 01 покрыт целиком |
| 8. Сила планет | `show-bala` | ✅ Шадбала со всеми компонентами, Вимшопака, Вайшешикамша, Ишта, Кашта |
| 9. Сила домов | `derive.house_strength` | ⚠️ бхава-балы у сайта нет и она не реконструируется (см. ниже). Вместо неё три прослеживаемых показателя на дом: бинду САВ, шадбала управителя, дришти на дом |
| 10. Аштакаварга | `show-info.ashtakavarga` | ✅ SAV + 8 BAV. ❌ шодханы, пинды, какшья, прастара — считаются из BAV, класс B |
| 11. Йоги | `show-yogas` | ✅ название, категория, планеты, эффект и условие |
| 12. Авастхи | `show-avasthas` | ✅ баляди, джаградади, диптади, шаянади |
| 13. Спец. состояния | `show-info.position` | ✅ шире, чем ожидалось: варготтама, гандантa, мритью-бхага, пушкара-навамша, **марана-карака-стхана**, карака-бхава-нашая, сандхи, диг-бала. ❌ нича-бханга, сожжение, кендрадхипати-доша |
| 14. Даши | `show-dasha` | ✅ 6 систем × уровни 1–4, границы с точностью до минуты. Калачакры на сайте нет |
| 15. Варшапхала | — | ❌ `403`, платно → всегда в MISSING_DATA |
| 16. Транзиты | `show-sade-sati` | ⚠️ транзиты `403` платно; саде-сати ✅ реализована |
| 17. Бхава-чалита, аргала | `show-bhava`, `get-aspects`, `get-argala` | ✅ по 12 знаков аспектов и аргалы. Ловушка: для знака Кету сайт меняет местами аргалу и виродху |
| 18. Панчанга | `show-other.panchanga` | ✅ |
| 19. Дополнительно | кэш `raw/*.json` | ✅ раздел отсылает к полной записи ответов, называет части, которые таблицы не разворачивают, и перечисляет файлы вне текущего плана. `show-current-periods` — единственный нереализованный парсер; текущий период выводится из даш |

Итого после фазы 2: **из 19 разделов закрыты 15, частично 2, недоступны 2.**
Недоступное — варшапхала и транзиты (платные) и бхава-бала (сайт её не считает
вовсе). Единственный нереализованный парсер — `show-current-periods`, и он не
критичен: текущий период вычисляется из уже собранных даш.

### Что останется недоступным навсегда

Варшапхала, транзиты, совместимость, мухурта — платные, отвечают `403`. Их
место — в `MISSING_DATA.md` с честной причиной, а не в имитации глубины. Промпт
05 при этом теряет только слой проверки: даши он строит на `show-dasha`, а
транзиты в нём и так объявлены дополнительным слоем, а не основой.

## 7. Ограничения, которые определяют дизайн

**Нагрузка на сайт.** Полный сбор — примерно 20 варг × 2–3 действия плюс даши по
шести системам: порядка 70–90 запросов на одну карту, каждый заставляет сайт
считать. Отсюда два жёстких требования: кэш `raw/*.json` на диске (повторный
запуск не ходит в сеть) и троттлинг между запросами. Заявленная мощность сайта —
35 000 карт в день, злоупотреблять нечестно и недальновидно.

**Язык.** `vedic-horo.ru` периодически отдаёт JS-заглушку и роняет сессию, `.com`
работает стабильно. Поэтому технический слой собираем с `.com` на английском:
коды планет, домов и знаков всё равно читаются из CSS-классов и одинаковы в
обоих языках. Клиентский текст пишет модель по-русски — русские подписи сайта
для этого не нужны. Текстовые интерпретации сайта (только на `.ru`) не нужны
тем более: их место занимает разбор.

**Кэш ≠ фиксация.** Если время рождения ректифицировали, кэш карты
инвалидируется целиком — ключ кэша включает все параметры карты.

## 8. Дорожная карта

**Фаза 0 — сделано.** Методика в репозитории: 10 промптов, README, шаблон
вёрстки.

**Фаза 1 — `jyotish collect`. Сделано.** Этап 01 целиком: карта на входе →
`raw/*.json` + `01_RAW_DATA.md` + `01_MISSING_DATA.md` + сводка для этапа 02.
План — 97 запросов; кэш делает повторный запуск бесплатным и возобновляемым.

**Фаза 2 — пять парсеров в `vedic-parser`. Сделано.** `show-yogas`,
`show-avasthas`, `show-bhava`, `show-sade-sati`, `get-aspects`/`get-argala`.
Не реализован только `show-current-periods`, и он не нужен: текущий период
выводится из даш.

**Фаза 3 — скилл и команды.** `SKILL.md` с правилами ведения разбора, сводки как
файлы, `patterns.json`, слэш-команды на каждый этап. С этого момента
копипаст между этапами исчезает.

**Фаза 4 — арифметика и гейты.** `soul_path.py` (веса, подшкалы, таблица
вкладов), `validate.py` (27 пунктов чек-листа + машинно-проверяемые правила
README: три шкалы не смешаны, у каждого паттерна ≥3 слоя, шкала пути души только
в своей главе).

**Фаза 5 — сборка PDF.** `report.py`: markdown этапа 09 → шаблон 11 → wkhtmltopdf
→ рендер страниц в картинки для просмотра глазами, как требует Промпт 10.

**Фаза 6 — производные, класс B. Частично сделано.** `derive.py` считает
арудхи (A1–A12, AL, UL, Дара-пада), диспозиторы с паривартаной и чара-караки со
сверкой против сайта. Остаётся: шодханы и пинды аштакаварги, нича-бханга,
сожжение, кендрадхипати-доша.

## 9. Открытые вопросы

1. **`00_README` в части «этап 01 ручной» устарел** — правку надо внести
   осознанно, вместе с решением, остаётся ли ручной путь как запасной (он нужен:
   парсер зависит от живого стороннего сайта).
2. **Промпт 01 §9 требует бхава-балу, которой не существует** в доступных
   данных. Раздел надо переписать под то, что есть: геометрия бхава-чалиты плюс
   дрик-бала на дома.
3. **Один источник данных — слабое место.** Сверка с независимым расчётом
   (`jyotish crosscheck`, jyotishganit на эфемеридах NASA JPL) на двух картах
   подтвердила положения до 0.02′, накшатры, пады и САВ по всем домам.
   Единственное расхождение, которое она объявила — айанамша vedic-horo, — было
   **ошибкой самой сверки**: `ecliptic_latlon()` без `epoch=` отвечает в
   эклиптике J2000, а айанамша отсчитывается от равноденствия на дату карты, и
   разница ровно равна прецессии с 2000 года (16.6′ для 1980-й). Сайт прав.
   Второе расхождение — Лагна на 16′ — оказалось той же ошибкой на стороне
   jyotishganit: библиотека вычитает из тропической Лагны заявленную (J2000)
   айанамшу, а из планет — фактическую. Сверка теперь объясняет это сама.
   Отсюда ответ на вопрос «считать ли локально вместо сайта»: библиотека
   годится как второй источник и для чувствительности, но не как первый,
   пока её Лагна не исправлена и не сверены варги, силы и даши.
   Открыто: делать ли сверку обязательной частью этапа 01. Вывод, который
   остаётся: проверяющий тоже требует проверки, потому что ложное расхождение
   обходится дороже, чем отсутствие сверки.
4. **Бхава-бала: считать ли самим?** Решено — нет. Бхавадхипати-бала есть
   напрямую, но Бхава-Диг-бала требует классической таблицы, а формулу
   Бхава-Дришти-балы не удалось воспроизвести: собственная дрик-бала планет у
   сайта не выводится из его же матрицы аспектов (проверено, невязка не
   объясняется ни весами натур, ни постоянным множителем). Реализации в JHora,
   Parashara's Light и прочих расходятся между собой, так что «правильного»
   числа всё равно нет. Вместо него — три показателя, которые не складываются.
   Если появится источник, которому доверяет астролог, вопрос можно открыть
   заново.
5. **Лимит запросов сайта.** Полный сбор — 97 запросов, и на сплошном проходе
   сайт отвечает 429 с многоминутным `Retry-After`. Сейчас сборщик в этом
   случае честно останавливается и продолжает с кэша при следующем запуске.
   Открыто: какой `throttle` действительно безопасен и не стоит ли разносить
   сбор на два прохода.
6. **Шаблон 11** покрывает обложку, оглавление и 4 из 23 разделов отчёта —
   остальные типы блоков придётся дорисовать в фазе 5.
7. **Где хранятся `clients/`** при работе с несколькими астрологами: локально,
   в приватном репозитории или в зашифрованном виде.
